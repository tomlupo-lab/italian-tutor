# Tasks

## Next

- Audit and trim seeded inventory.
- Rebalance default card content toward high-frequency A1-B1 chunks and pattern-friendly examples.
- Check seed metadata consistency across `level`, `tag`, `phase`, `patternId`, and `domain`.
- Remove or rewrite low-yield, awkward, or overly niche seed entries.

## After That

- Improve recovery-card normalization.
- Prefer reusable chunk-first repair cards over artifact-like mistake traces.
- Review whether recovery output maps cleanly to the current pattern lanes.

## Notes

- Playwright instability is addressed by running e2e against `next build && next start`.
- Current smoke coverage is stable enough to support content work next.
